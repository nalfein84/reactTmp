import { StyleSheet } from "react-native";

const Container = StyleSheet.create({
    relative: {
        position: "relative"
    },
    absolute: {
        position: "absolute"
    }
})

export { Container }
